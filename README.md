# SparkKeys

Beginner piano web prototype.

Open `index.html` with the `css` and `js` folders beside it (computer browser), or use GitHub Pages.

iPhone cannot run a downloaded HTML folder as a site.

Audio: recorded grand samples, one dynamic per pitch. Velocity is gain and filter, not four hammer layers.
